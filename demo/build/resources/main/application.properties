spring.application.name=product-service
spring.data.mongodb.uri=mongodb+srv://22520988:22520988@cluster0.zeztdiz.mongodb.net/apidesign?retryWrites=true&w=majority
server.port=5050

# Chỉ định không trả về stack trace khi có lỗi
server.error.include-stacktrace=never

# Đăng ký với Eureka Server 
eureka.client.service-url.defaultZone=http://eureka:8761/eureka
eureka.instance.prefer-ip-address=true
eureka.instance.lease-renewal-interval-in-seconds=10
eureka.instance.lease-expiration-duration-in-seconds=30

# Cấu hình Redis (nếu cần)
spring.data.redis.host=redis
spring.data.redis.port=6379